<% include partials/header %>

<body>
	<div id="top">
		<h1>Flag Search</h1>
		<span id="lyrics"></span>
	</div>
	
	<div id="middle">		
		<!-- <span id="result"></span> -->
		<form id="searchForm" action="/search/ <%= entry._id %>" method="POST">
		    <input type="text" name="searchImage" placeholder="Enter Country">
		    <button id="searchButton">Search</button>
		</form>
		<button id="back"><a href="/">Back</a></button>
	</div>

	<div id="bottom">
		<!--Wenn richtig eingegeben, dann soll dieses DIV das Bild des eingegebenen Landes darstellen.-->
		<!--s wäre jetzt die id-->
		<%var s = "togo" %>
    	<%var s_adjusted = s.toLowerCase()%>
		<%var results ={}%>
    	<%var counter = 0;%>
    	<%for (key in data){%>
        	<%var key_adjusted = key.toLowerCase()%>
        	<%if(key_adjusted.includes(s_adjusted)&& s_adjusted!== ""){%>
            	
            	<%results[key] = data[key]%>
            	<%found = true;%>
            	<%counter = counter + 1%>
            <% } %>
        <% } %>
		<p><%=results["Togo"]%></p>
		
	 <%	if(counter == 1){ %>
     <%   	for(key in results){ %>
            	<p><%=results[key]%></p>
     <%   	} %>
     <% } %>
		
	    <p id="searchedImageText"></p>
		
	</div>
	
	
	
	<!--<p id="searched" style="display:none"></p>-->
	<p> <%= data["Afghanistan"] %></p>
	
<!--<script type="text/javascript" src="Flag_Search.js"></script>-->

<% include partials/footer %>

